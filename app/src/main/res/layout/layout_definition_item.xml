<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/constrainedLayoutDesc"
    android:layout_width="match_parent"
    android:layout_height="150dp"
    android:background="@drawable/rect"
    android:elevation="6dp"
    android:layout_marginHorizontal="5dp"
    android:layout_marginVertical="15dp"
    android:stateListAnimator="@animator/nav_default_enter_anim"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    android:minHeight="40dp"
    android:padding="4dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16dp"
        android:text="Definition"
        android:textStyle="bold"
        android:textColor="@color/dark_grey"
        android:layout_centerHorizontal="true"
        android:id="@+id/textViewDefinition"/>

    <TextView
        android:id="@+id/textViewDefinitionBody"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:autoSizeTextType="uniform"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="3dp"
        android:layout_below="@+id/textViewDefinition"
        android:textSize="16dp"/>

</RelativeLayout>